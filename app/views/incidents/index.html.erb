<div class='logo_overlay d-flex justify-content-center p-4'>
<%= image_tag 'logo_incidents.png', style: 'width: 100%; -webkit-filter: drop-shadow(5px 1px 5px rgba(123, 127, 131, 0.5));'%>
</div>
<div id='map' style='width: 100%; height: 100vh;'
  data-controller="map"
  data-map-markers-value="<%= @markers.to_json %>"
  data-map-api-key-value="<%= ENV['MAPBOX_API_KEY'] %>"></div>
<div data-controller="open-incidents">
  <% @incidents.each do |incident| %>
  <%= render "incident", incident: incident %>
  <% end %>
</div>
